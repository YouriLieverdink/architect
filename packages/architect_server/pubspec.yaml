name: architect_server
version: 0.1.0-dev.1
publish_to: none

environment: { sdk: ">=2.18.0 <3.0.0" }
dependencies:
  {
    architect: { path: ../../packages/architect },
    dotenv: ^4.0.1,
    equatable: ^2.0.5,
    http: ^1.2.0,
    sealed_unions: ^3.0.3+1,
    shelf_plus: ^1.5.0,
  }
dev_dependencies: { lint: ^2.0.20 }
