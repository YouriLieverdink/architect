name: server
version: 0.1.0-dev.1
publish_to: none

environment: { sdk: ">=2.17.0 <3.0.0" }
dependencies:
  {
    orchestrator: ^2.2.0,
    shared: { path: ../../packages/shared },
    shelf_plus: ^1.5.0,
  }
dev_dependencies: { lint: ^1.10.0, test: ^1.21.4 }
